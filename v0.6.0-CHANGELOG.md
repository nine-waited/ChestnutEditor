# Chestnut Editor v0.6.0

---

## English

A desktop editor for local Markdown and Excalidraw files — shared save modes, safer Excalidraw persistence, and smoother split / tab workflows.

Chestnut Editor reads and writes plain files in a folder you choose. No proprietary database, no cloud required.

### Download

| Item | Details |
|------|---------|
| **Installer** | `Chestnut_0.6.0_x64-setup.exe` (Windows x64, NSIS) |
| **Requirements** | 64-bit Windows 10+; WebView2 (downloaded during install if missing) |
| **Default vault** | `~/.chestnut` on first launch |

**Build output:**

`apps/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Chestnut_0.6.0_x64-setup.exe`

### What's New in v0.6.0

#### Save modes (Markdown + Excalidraw)

- Settings → Notes: **Every 15s autosave** (default) or **Realtime save**
- Shared by Markdown notes and Excalidraw drawings; Ctrl/Cmd+S still works
- Save-status badge (icon + text, no chrome): notes in the title bar, drawings at the bottom center
- Interval mode: unsaved solid dots on tabs; closing warns with a red confirm button
- 15s tick only writes when the buffer is actually dirty

#### Excalidraw hardening

- Scene snapshots are path-bound so switching drawings cannot overwrite another file
- Second drawing opens a **new tab** instead of replacing the current one
- Narrow split panes keep the **desktop** chrome (no accidental mobile layout)
- Save-status layer sits above the canvas UI

#### Split & drag

- Emptying a pane (close all / close last tab / drag last tab away) **exits split** and merges back to single pane
- Drag file-tree items onto the **tab bar** to open, or onto the **right split drop zone** to open and enter split
- Outline drawer defaults sync when split auto-exits

#### Editor fixes

- Live Markdown list bullets no longer vanish after refresh
- Milkdown no longer auto-escapes Markdown punctuation on save
- Bold / highlight no longer keep extending when typing after formatted text
- Source editor top lines are no longer covered by the note title bar
- Deleted notes no longer reappear from keep-alive editor autosave
- Undo keep-alive cache reduced from 10 files to 5
- Paste, split-drag, file locate, and Markdown clipboard fidelity improvements

### Upgrading from v0.5.0

- No vault migration required
- Save-mode preference is stored locally (default remains 15s autosave)
- Split behavior change: emptying one pane now exits split (v0.5.0 kept split open)

### Highlights from v0.5.0 (still included)

- Left / right split, pinned area, outline drawers, richer file-tree multi-select
- Global search; always-on-top; image copy / undo restore

---

## 简体中文

面向本地 Markdown 与 Excalidraw 的桌面编辑器 —— 统一保存方式、更稳妥的绘图落盘，以及更顺手的分屏 / 标签流程。

Chestnut Editor 在你选择的文件夹中直接读写普通文件，无需专有数据库，也不依赖云端。

### 下载

| 项目 | 说明 |
|------|------|
| **安装包** | `Chestnut_0.6.0_x64-setup.exe`（Windows x64，NSIS） |
| **系统要求** | 64 位 Windows 10 及以上；缺少 WebView2 时安装程序会自动下载 |
| **默认知识库** | 首次启动打开 `~/.chestnut` |

**构建产物：**

`apps/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Chestnut_0.6.0_x64-setup.exe`

### v0.6.0 新内容

#### 保存方式（Markdown + Excalidraw）

- 设置 → 笔记：**每 15 秒自动保存**（默认）或 **实时保存**
- Markdown 与 Excalidraw 共用同一开关；仍可用 Ctrl/Cmd+S
- 保存状态提示（图标 + 文字、无边框背景）：笔记在标题栏，绘图在画布底部居中
- 间隔模式：未保存 Tab 显示实心点；关闭时红色确认按钮
- 15 秒检查仅在内容确实未保存时写盘

#### Excalidraw 加固

- 场景快照绑定路径，切换绘图不会互相覆盖
- 打开第二个绘图会 **新开 Tab**，不再覆盖当前标签
- 分屏变窄时仍保持 **桌面版** 排版（不再误切移动端布局）
- 保存提示图层置于画布 UI 之上

#### 分屏与拖拽

- 一侧被清空（关闭全部 / 关最后一个 / 拖走最后一个）会 **退出分屏** 并合并回单屏
- 文件栏可拖到 **Tab 栏** 打开，或拖到 **右侧分屏投放区** 打开并进入双屏
- 自动退出分屏时同步目录抽屉默认状态

#### 编辑器修复

- 实时预览下列表圆点刷新后不再消失
- 保存时不再自动转义 Markdown 标点
- 加粗 / 高亮在格式后继续输入时不再误续上
- 源码模式顶部行不再被标题栏挡住
- 删除笔记后，保活编辑器不会把文件写回
- 撤销保活缓存由 10 降为 5
- 粘贴、分屏拖拽、定位文件、剪贴板等体验改进

### 从 v0.5.0 升级

- 无需迁移知识库
- 保存方式偏好保存在本地（默认仍为每 15 秒自动保存）
- 分屏行为变更：一侧清空后会退出分屏（v0.5.0 会保持分屏）

### v0.5.0 亮点（仍保留）

- 左右分屏、置顶区、目录抽屉、文件树多选
- 全局搜索、窗口置顶、图片复制 / 撤销恢复
