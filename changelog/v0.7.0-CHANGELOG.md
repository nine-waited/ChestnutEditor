# Chestnut Editor v0.7.0

---

## English

A desktop editor for local Markdown and Excalidraw files — Mermaid previews, view-only notes, same-file split editing, and richer export / file-tree polish.

Chestnut Editor reads and writes plain files in a folder you choose. No proprietary database, no cloud required.

### Download

| Item | Details |
|------|---------|
| **Installer** | `Chestnut_0.7.0_x64-setup.exe` (Windows x64, NSIS) |
| **Requirements** | 64-bit Windows 10+; WebView2 (downloaded during install if missing) |
| **Default vault** | `~/.chestnut` on first launch |

**Build output:**

`apps/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Chestnut_0.7.0_x64-setup.exe`

### What's New in v0.7.0

#### Mermaid in Markdown

- Code blocks marked as Mermaid render as diagrams in Live mode
- Preview stays inside the note; broken diagrams show a clear error state

#### View-only & split same-file Markdown

- Title-bar **Only view** toggle: browse without editing (title, body, image delete)
- Split panes may open the **same Markdown file** twice; the later copy defaults to view-only
- Cancelling view-only on one side forces the twin into view-only; both sides flush then reload from disk
- Closing one copy or exiting split merges back to a single editable tab
- View-only hides the center save badge and the tab unsaved solid dot
- Non-Markdown types (Excalidraw / image / PDF) remain exclusive across panes

#### Tabs & reload

- Markdown tab context menu: **Refresh** reloads the note from disk (confirms when discarding unsaved interval buffers)

#### Export & icons

- Export Markdown notes as a **ZIP** (note + `_pic` assets); ZIP letter icon in the file tree
- PDF letter mark icon aligned with Markdown / ZIP styling
- App icons and README screenshots refreshed

#### Editor & layout fixes

- Live opening heading no longer loses top margin when selecting text (virtual-cursor `:first-child` clash)
- Live / source views no longer diverge when undo history blocked external sync
- Interval save in split view: per-pane keep-alive isolation so panes cannot overwrite each other
- Sidebar: collapse tab stays clickable above Excalidraw; vault path remains visible when collapsed; reveal-active-file button removed (auto-locate kept)
- File-tree focus can clear on empty clicks and restore from the editor
- Default vault README prefers Chinese; in-app links open the English README

### Upgrading from v0.6.0

- No vault migration required
- New paired view-only behavior only applies when the same Markdown path is open on both split panes

### Highlights from v0.6.0 (still included)

- Shared 15s / realtime save modes for Markdown + Excalidraw
- Safer Excalidraw path-bound snapshots; emptying a pane exits split

---

## 简体中文

面向本地 Markdown 与 Excalidraw 的桌面编辑器 —— Mermaid 预览、仅查看笔记、分屏同文件编辑，以及更完整的导出与文件栏体验。

Chestnut Editor 在你选择的文件夹中直接读写普通文件，无需专有数据库，也不依赖云端。

### 下载

| 项目 | 说明 |
|------|------|
| **安装包** | `Chestnut_0.7.0_x64-setup.exe`（Windows x64，NSIS） |
| **系统要求** | 64 位 Windows 10 及以上；缺少 WebView2 时安装程序会自动下载 |
| **默认知识库** | 首次启动打开 `~/.chestnut` |

**构建产物：**

`apps/desktop/src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/Chestnut_0.7.0_x64-setup.exe`

### v0.7.0 新内容

#### Markdown 中的 Mermaid

- 标记为 Mermaid 的代码块在实时模式下渲染为图表
- 预览留在笔记内；语法错误会给出明确提示

#### 仅查看与分屏同文件 Markdown

- 标题栏 **仅查看**：浏览时不可编辑（标题、正文、图片删除）
- 分屏两侧可打开 **同一 Markdown**；后打开的默认仅查看
- 取消一侧仅查看时，对侧自动变为仅查看；切换前会落盘并可刷新两侧
- 关掉其中一份或退出分屏后，合并为单个可编辑标签
- 仅查看时隐藏中间保存提示，以及 Tab 上的未保存实心点
- 非 Markdown（Excalidraw / 图片 / PDF）仍保持跨屏互斥

#### 标签与刷新

- Markdown 标签右键 **刷新**：从磁盘重新加载（间隔保存且有未保存更改时会确认是否丢弃）

#### 导出与图标

- 支持将笔记导出为 **ZIP**（正文 + `_pic` 资源）；文件栏显示 ZIP 字母图标
- PDF 字母图标风格与 Markdown / ZIP 对齐
- 应用图标与 README 截图更新

#### 编辑器与布局修复

- 实时模式首行标题选中文本时不再丢失顶部间距（虚拟光标与 `:first-child` 冲突）
- 撤销历史不再阻断外部同步，避免实时 / 源码内容分叉
- 分屏间隔保存：按屏隔离保活实例，避免互相覆盖
- 侧栏：收起条在 Excalidraw 之上可点；收起后仍可见知识库路径；去掉「定位当前文件」按钮（保留自动定位）
- 文件栏可点击空白取消选中，也可从编辑器恢复焦点
- 默认知识库 README 优先中文；应用内链接打开英文 README

### 从 v0.6.0 升级

- 无需迁移知识库
- 仅当同一 Markdown 在左右分屏各开一份时，才会启用配对仅查看

### 仍包含的 v0.6.0 能力

- Markdown + Excalidraw 共用 15 秒 / 实时保存
- 更稳妥的 Excalidraw 路径绑定快照；清空一侧即退出分屏
